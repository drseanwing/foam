# =============================================================================
# FOAM N8N Docker Compose Environment Variables
# =============================================================================
# Copy this file to .env in the same directory and fill in your values
# DO NOT commit .env to version control
#
# For full configuration options, see config/n8n-env.example
# =============================================================================

# N8N Authentication
N8N_USER=admin
N8N_PASSWORD=change_me_to_secure_password

# N8N Host Configuration (use localhost for dev, your domain for production)
N8N_HOST=localhost

# PostgreSQL Configuration
POSTGRES_USER=foam
POSTGRES_PASSWORD=change_me_to_secure_password

# API Keys (Required for LLM operations)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
OPENAI_API_KEY=sk-your-key-here
SERP_API_KEY=your-serp-api-key-here

# Optional: NCBI API Key (increases PubMed rate limits)
NCBI_API_KEY=your-ncbi-api-key-here

# Optional: Slack Webhook for notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url

# =============================================================================
# Production-Only Variables (required for docker-compose.prod.yml)
# =============================================================================

# Domain Configuration
DOMAIN=foam.example.com
LETSENCRYPT_EMAIL=admin@example.com

# Timezone (optional, defaults to UTC)
TIMEZONE=Australia/Brisbane

# Traefik Dashboard Authentication
# Generate with: echo $(htpasswd -nb admin password) | sed -e s/\\$/\\$\\$/g
# Or use: docker run --rm httpd:alpine htpasswd -nb admin password | sed -e s/\\$/\\$\\$/g
TRAEFIK_BASIC_AUTH=admin:$$apr1$$abc123$$xyz

# Ollama API Authentication (optional, defaults to TRAEFIK_BASIC_AUTH if not set)
OLLAMA_BASIC_AUTH=admin:$$apr1$$abc123$$xyz

# GPU Configuration (optional)
# Set to 0 to disable GPU, or number of GPUs to use
NVIDIA_GPU_COUNT=1
